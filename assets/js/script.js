let menuButton = document.getElementsByClassName("menu-icon")[0];
let menu = document.getElementsByClassName("header-right")[0];
menuButton.addEventListener('click', function() {
    menu.classList.toggle("show-menu");
    // if(menu)
});